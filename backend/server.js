// const http = require('http'); // Import du module Node HTTP pour permetttre les requettes HTTP

// const server = http.createServer((req, res) => { // Creation d'un serveur qui reçoit des requetes (req) et envoie une réponse (res)
//     res.end('réponse du server'); 
// });

// server.listen(process.env.PORT || 3000); // Le serveur écoute sur le port 3000 ou un port défini dans les variables d'environnement
console.log('test');